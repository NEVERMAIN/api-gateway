<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.NEVERMAIN.gateway.center.infrastructure.dao.IApplicationInterfaceDao">


    <resultMap id="ApplicationInterfaceMap"
               type="io.github.NEVERMAIN.gateway.center.infrastructure.dao.po.ApplicationInterface">
        <id column="id" property="id"/>
        <id column="system_id" property="systemId"/>
        <id column="interface_id" property="interfaceId"/>
        <id column="interface_name" property="interfaceName"/>
        <id column="protocol_type" property="protocolType"/>
        <id column="interface_version" property="interfaceVersion"/>
        <id column="create_time" property="createTime"/>
        <id column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO application_interface(system_id, interface_id, interface_name, protocol_type , interface_version, create_time, update_time)
        VALUES (#{systemId}, #{interfaceId}, #{interfaceName}, #{protocolType} , #{interfaceVersion}, now(), now())
    </insert>

    <update id="update">
        UPDATE application_interface
        SET interface_name    = #{interfaceName},
            protocol_type     = #{protocolType},
            interface_version = #{interfaceVersion},
            update_time       = now()
        WHERE system_id = #{systemId}
          AND interface_id = #{interfaceId}
    </update>

    <select id="queryApplicationInterfaceList"
           resultMap="ApplicationInterfaceMap">
        SELECT system_id, interface_id, interface_name, protocol_type ,  interface_version
        FROM application_interface
        WHERE system_id = #{systemId}
    </select>

    <select id="queryApplicationInterfaceListBySystemIds"
            resultMap="ApplicationInterfaceMap">
        SELECT system_id, interface_id, interface_name, protocol_type ,interface_version
        FROM application_interface
        WHERE system_id IN
        <foreach collection="systemIdList" index="idx" item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>


    <select id="queryApplicationInterfaceListByPage"
            parameterType="io.github.NEVERMAIN.gateway.center.types.common.OperationRequest"
            resultMap="ApplicationInterfaceMap">
        SELECT id, system_id, interface_id, interface_name, protocol_type ,interface_version , create_time , update_time
        FROM application_interface
        <where>
            <if test="data != null and data.systemId != '' ">
                AND system_id = #{data.systemId}
            </if>
            <if test="data != null and data.interfaceId != ''">
                AND interface_id = #{data.interfaceId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{pageStart}, #{pageEnd}
    </select>


    <select id="queryApplicationInterfaceListCountByPage"
            parameterType="io.github.NEVERMAIN.gateway.center.types.common.OperationRequest"
            resultType="java.lang.Integer">
        SELECT COUNT(id)
        FROM application_interface
        <where>
            <if test="data != null and data.systemId != '' ">
                AND system_id = #{data.systemId}
            </if>
            <if test="data != null and data.interfaceId != ''">
                AND interface_id = #{data.interfaceId}
            </if>
        </where>
    </select>

    <select id="queryApplicationInterface"
            resultMap="ApplicationInterfaceMap">
        SELECT system_id, interface_id, interface_name, protocol_type ,interface_version
        FROM application_interface
        WHERE system_id = #{systemId}
          AND interface_id = #{interfaceId}
    </select>


</mapper>
